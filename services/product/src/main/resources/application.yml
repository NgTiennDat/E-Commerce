spring:
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: product-service
redis:
  host: 127.0.0.1
  port: 6379

# Seeder tắt mặc định — bật bằng seeder.enabled=true khi cần seed data
seeder:
  enabled: false